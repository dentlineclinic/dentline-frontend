## TODO - Patient profile page phone/email lock feature

- [x] Locate patient profile page: `app/(patient)/patient/profile/page.tsx`
- [x] Update `handleSubmit` payload to only include the editable login identifier based on `lastVerificationType`.

- [x] Keep disabled inputs as UI-only (still allow displaying current values).

- [ ] Verify locally by logging in via phone vs email and saving profile.

